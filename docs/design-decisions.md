# Design Decisions

_Awaiting project context. Run intake via `docs/project-intake.md` to populate this section._

Locked decisions only — the source of truth for anything settled (architecture, tooling, conventions, values that shouldn't be re-litigated). Not a place for open questions, risks, or deferred alternatives — those belong in `docs/backlog.md`. Kept in sync by the `update-claude` skill.
